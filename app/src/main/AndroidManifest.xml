<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heshidai.app.actiondemo">

    <application android:name=".BaseApplication">
        <!--默认取的就是包名，故无特殊需要不需要设置该值-->
        <meta-data
            android:name="domain"
            android:value="comheshidaiappactiondemo" />
    </application>
</manifest>
